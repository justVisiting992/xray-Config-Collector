name: Collector
on:
  workflow_dispatch:
  schedule:
    - cron: "0 */2 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.22'

    - name: Download GeoIP Database
      run: |
        wget -q https://github.com/P3TERX/GeoLite.mmdb/raw/download/GeoLite2-Country.mmdb -O Country.mmdb

    - name: Build and run Collector
      run: |
        go mod init github.com/justVisiting992/xray-Config-Collector || true
        go mod tidy
        go build -o myapp main.go
        ./myapp -sort
    
    - name: Generate Workflow Summary
      if: always()
      run: |
        if [ -f summary.md ]; then
          cat summary.md >> $GITHUB_STEP_SUMMARY
        fi

    - name: Commit and Push Changes
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"
        # Adding text files AND the new report.md
        git add *.txt report.md
        git diff --quiet && git diff --staged --quiet || (git commit -m "📊 Report Updated: $(date '+%Y-%m-%d %H:%M')" && git push origin main)
